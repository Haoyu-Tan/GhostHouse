#ifndef GLOBAL_VAR_INCLUDED
#define GLOBAL_VAR_INCLUDED

enum sModelType {
	floorr, wall, door, key, bookcase, chest, stand, torch, table, bbookcase, pileBone,
	desk, candle, health_base, health_bar
};

enum dModelType {whiteGhost, blueGhost, gun, greenGhost}; //type bullet


#endif // !GLOBAL_VAR_INCLUDED

